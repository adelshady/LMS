@model IEnumerable<LMS.Models.Announcement>
@{
    ViewData["Title"] = "AnnounceIndex";
}

@*<p>
    <a asp-action="AnnounceCreate" class="btn btn-primary text-white">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.body)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShowUntil)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.body)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Image)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ShowUntil)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedAt)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Roles)
                </td>
                @*<td>
                    <a class="btn btn-primary text-white">
                        Edit
                    </a> |
                    <a  class="btn btn-success text-white">
                        Details
                    </a> |
                    <a class="btn btn-danger text-white">
                        Delete
                    </a>
                </td>*@
            </tr>
        }
    </tbody>
</table>
